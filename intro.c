#include <stdio.h>
int main(){
int number,cube;
printf("Enter a number to calculate it's cube\n");
scanf("%d",&length);
cube=(number*number*number);
if((number%2)!=0 ){
    printf("cannot calculate the cube\n");
}
else{
    printf("%d=%d*%d*%d","Cube\n");
}
}